# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Swagger - Root Resources Listing
GET     /resources.json             controllers.swagger_java.ApiHelpController.getResources

# Pet - Resources
GET     /pet.json                   controllers.swagger_java.ApiHelpController.getResource(path = "/pet")

GET     /pet.json/:id               controllers.PetApiController.getPetById(id)
